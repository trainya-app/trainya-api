"use strict";
/* eslint-disable no-undef */
describe('Crete product category', () => {
    it('should be 4', () => {
        const sum = 2 + 2;
        const result = 4;
        expect(sum).toBe(result);
    });
});
